spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
    
  jpa:
    show-sql: true

eureka:
  client:
    service-url.defaultZone: http://localhost:2222/eureka
